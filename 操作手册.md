# slip-detection 项目 - 具体操作手册

> 状态：项目骨架已建好（`C:\Users\30352\WorkBuddy\2026-09-15-20-49-13\slip-detection\`）
> 目标：在 2-3 天内把"三版散乱的代码"升级为"可一键复现的工程仓库"

---

## 🚀 今晚 2 小时：把基础环境跑起来

### Step 1：装 WSL2 Ubuntu（20 分钟）

打开 **PowerShell（管理员）**，执行：

```powershell
wsl --install
```

会自动装 Ubuntu 默认版。装完会提示**重启电脑**，重启后设置 Ubuntu 用户名和密码。

验证：
```powershell
wsl --status
```
应该显示 `默认分发版: Ubuntu`。

---

### Step 2：在 Ubuntu 里装 Miniconda + PyTorch（30 分钟）

打开 Ubuntu 终端（开始菜单搜 "Ubuntu"），逐条执行：

```bash
# 2.1 下载 Miniconda（约 100MB）
cd ~
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh

# 2.2 安装（按 Enter 一路到 yes）
bash Miniconda3-latest-Linux-x86_64.sh -b -p $HOME/miniconda3

# 2.3 激活 conda
~/miniconda3/bin/conda init bash
source ~/.bashrc

# 2.4 创建环境
conda create -n slip python=3.10 -y
conda activate slip

# 2.5 装 PyTorch（CPU 版足够，研一论文阶段不用 GPU）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# 2.6 装其它依赖
pip install numpy scipy scikit-learn imbalanced-learn matplotlib pyyaml

# 2.7 验证
python -c "import torch; print('torch', torch.__version__)"
python -c "import imblearn; print('imblearn OK')"
```

应该看到：
```
torch 2.x.x
imblearn OK
```

---

### Step 3：把 h5 数据搬过来（10 分钟）

在 **Git Bash**（你现在这个窗口）执行：

```bash
# 3.1 看一眼数据长什么样
ls "/c/Users/30352/Desktop/Database/slip_detection_dataset_2021/"
```

你应该看到类似 `brush_*.h5`、`screwDriver_(1)_*.h5` 这种文件。

```bash
# 3.2 软链接方式（推荐，不占空间）
ln -s "/c/Users/30352/Desktop/Database/slip_detection_dataset_2021" \
      "/c/Users/30352/WorkBuddy/2026-09-15-20-49-13/slip-detection/data_raw"

# 3.3 验证
ls "/c/Users/30352/WorkBuddy/2026-09-15-20-49-13/slip-detection/data_raw/"
```

应该看到所有 h5 文件。

> 如果软链接不工作（Windows 上偶尔有权限问题），用拷贝：
> ```bash
> cp -r "/c/Users/30352/Desktop/Database/slip_detection_dataset_2021/"* \
>       "/c/Users/30352/WorkBuddy/2026-09-15-20-49-13/slip-detection/data/"
> ```

---

### Step 4：GitHub 建仓 + 第一次 push（15 分钟）

**4.1 在 GitHub 网页上**：
1. 登录 GitHub
2. 右上角 `+` → `New repository`
3. Repository name: `slip-detection`
4. Description: `Tactile slip detection on BioTac SP dataset with LSTM/GRU + SMOTE + frequency-band features`
5. **不要勾** Add README / .gitignore / license（我们已经有了）
6. 点 `Create repository`

**4.2 在项目目录里初始化 git**：

```bash
cd "/c/Users/30352/WorkBuddy/2026-09-15-20-49-13/slip-detection"

# 1. 配身份（只配一次）
git config --global user.name "你的中文名"
git config --global user.email "你的GitHub邮箱"

# 2. 初始化
git init
git branch -M main

# 3. 关联远程仓（替换成你的用户名）
git remote add origin https://github.com/<你的用户名>/slip-detection.git

# 4. 第一次提交
git add .
git commit -m "feat: 初始项目骨架 + preprocess argparse 改造"

# 5. 推到 GitHub
git push -u origin main
```

如果 push 时弹出登录框，用 GitHub Personal Access Token（PAT），密码框里粘贴 token，不是 GitHub 密码。

> 没有 token？去 GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token。勾选 `repo`，生成后复制保存。

---

## ✅ 明天下午 2 小时：跑通 preprocess 验证改造成功

### Step 5：第一次跑 preprocess（15 分钟）

**5.1 进 WSL2 跑**（更接近论文实验的真实环境）：

```bash
# 在 Git Bash 里
wsl
cd /mnt/c/Users/30352/WorkBuddy/2026-09-15-20-49-13/slip-detection
conda activate slip
python src/preprocess.py
```

应该看到：
```
找到 h5 文件: 5 类共 XX 个
[brush]   (XX, 2500)  滑移率 XX.XX%
...
合并后: (XXXXX, 2500)  总滑移率 XX.XX%
已保存 processed_data/X_train.npy 等
```

如果报错，**最可能的原因**：

| 报错 | 解决 |
|---|---|
| `ModuleNotFoundError: No module named 'imblearn'` | 没装，回到 Step 2.6 |
| `FileNotFoundError: xxx.h5` | `data_raw/` 没链接好，回到 Step 3 |
| `KeyError: 'slip'` 或 `'slip_gt'` | h5 文件里 key 不对，打开一个看看 `python -c "import h5py; f=h5py.File('data_raw/xxx.h5','r'); print(list(f.keys()))"` |

**5.2 跑 SMOTE 版**：

```bash
python src/preprocess.py --use_smote
```

应该看到 SMOTE 后的滑移样本数变成原来 × 比例。

**5.3 把这两个跑完后的 commit**：

```bash
git add processed_data/ outputs/   # 如果没被 .gitignore
# 这两个目录已在 .gitignore 里，所以这一步会跳过

# 跑通成功的日志可以保存到 docs/
echo "preprocess baseline + smote 跑通于 $(date)" > docs/preprocess_test_log.md
git add docs/preprocess_test_log.md
git commit -m "test: 验证 preprocess.py baseline 和 smote 两个模式都能跑通"
git push
```

---

### Step 6：跟刘二大人 PyTorch 课（晚 1 小时）

打开 B 站搜 **"刘二大人 PyTorch 深度学习实践"**：
- 第 1 讲：线性回归
- 第 2 讲：基础 CNN（先看）
- 第 5 讲：循环神经网络（RNN/LSTM/GRU，重点！）
- 第 6 讲：分类（和你任务直接相关）

**跟着敲代码**，不要光看。每讲 30-40 分钟。

---

## 📅 本周内（4-5 天）做完这些事

| Day | 任务 | 时间 |
|---|---|---|
| Day 1 晚 | Step 1-4：WSL2 + conda + GitHub 跑通 | 2h |
| Day 2 晚 | Step 5：跑通 preprocess 两版 + commit | 2h |
| Day 2 晚 | Step 6：刘二大人 PyTorch 1-2 讲 | 1h |
| Day 3 晚 | 精读第一篇论文（用 docs/五问笔记模板.md） | 1.5h |
| Day 4 晚 | 刘二大人 PyTorch 5-6 讲（GRU 重点） | 1.5h |
| Day 5 白天 | 联系导师汇报 + 让他帮你看 GitHub | 30min |
| Day 5 晚 | 看下一节我会给你的 train.py 改造 | 30min |

---

## 🆘 卡住了怎么办？

按下面顺序排查：

1. **报错信息先复制给我看**（贴完整）
2. 自己 Google：报错最后一行 + "pytorch" / "wsl2"
3. 搜 B 站：报错关键词
4. 实在不行，放一晚上，第二天精力好再来

---

## 📊 完成进度自检

完成 Step 1-5 后，你应该有：

- [ ] WSL2 Ubuntu 装好
- [ ] conda 环境 `slip` 能激活
- [ ] `slip-detection/data_raw/` 下能看到 h5 文件
- [ ] GitHub 上能看到 `slip-detection` 仓库
- [ ] `python src/preprocess.py` 能跑通
- [ ] `python src/preprocess.py --use_smote` 能跑通
- [ ] 处理后的 `processed_data/*.npy` 文件生成

任意一项 ❌，告诉我哪一步卡住了。

---

**告诉我你现在卡在哪一步**，或者**直接说"我开始执行 Step 1"**——我等你的进度反馈。